# Remote runtime smoke trigger

This temporary pull-request-only file triggers the current Docker and Podman runtime smoke workflows on the correctly labelled `SQL_Lab` runners.

It is not intended for merge and will be discarded after the validation result is collected.
